# VisionAI — 온프레미스/SaaS 하이브리드 음성 AI 플랫폼 (레고블록 모듈 아키텍처)

> 주식회사 링버스(LNKBUS) · B2B 설계/기획 문서 v2.0 · 2026-08
> 기준 사양: 「온프레미스 실시간 다국어 STT/TTS & sLLM AI 플랫폼 개발 사양서 v1.0」

## 프로젝트 개요

**VisionAI**는 폐쇄망(Air-Gapped) 온프레미스와 SaaS 양쪽으로 공급 가능한
**엔터프라이즈 실시간 음성/텍스트 AI 플랫폼**이다.

- 핵심 도메인: **금융 AICC**(실시간 상담원 지원) + **공공 스마트 회의록**
- 실시간 다국어 STT/TTS, Micro-Rule 컴플라이언스 필터, sLLM 기반 실시간 TA(Agent Assist)/RAG
  지식 팝업, 자동 요약 파이프라인을 단일 플랫폼으로 제공
- 향후 **AI 가상상담원(아바타 상담)**으로 확장

### 핵심 아키텍처 전략

1. **레고블록 모듈(Composable & Billable Blocks)** — 모든 컴포넌트는 독립 배포·조립 가능한
   블록이며, 동시에 **견적서 라인 아이템·라이선스 게이팅 단위**다. 블록 조합(bundle)으로
   제품 패키지(AICC / 회의록 / 가상상담원)를 구성하고, 업셀은 라이선스 파일 재발급으로 완결
2. **Model-Agnostic 어댑터** — STT(Faster-Whisper↔Triton), TTS(CosyVoice/Kokoro),
   sLLM(vLLM↔상용 API)을 코드 수정 없이 핫스왑
3. **Dual-Profile + Sub-Second** — AICC(초저지연 Stereo/RTP)와 회의록(고정밀 Mono/File)
   동시 지원, STT→RAG 지식 팝업까지 1초 미만
4. **Complete Air-Gapped** — H/W Fingerprint 오프라인 DRM, KCMVP 암호화, 외부 호출 0건.
   동일 코드가 SaaS에서는 상용 API Provider로 동작

## 문서 구성

| 문서 | 내용 |
|------|------|
| [**00. 레고블록 모듈 카탈로그**](docs/00-module-catalog.md) | **단일 기준 문서** — 블록 정의·계약·조립 레시피·블록별 청구 모델 |
| [01. 제품 기획서](docs/01-product-plan.md) | 시장/타겟, 패키지 상품화, 가격·라이선스 모델, 로드맵 |
| [02. 시스템 아키텍처](docs/02-architecture.md) | End-to-End 구조, 실시간 파이프라인, 어댑터, 프로토콜 |
| [03. 배포 전략 (온프렘/SaaS)](docs/03-deployment-onprem-saas.md) | 에어갭 번들, K8s/Compose, 오프라인 DRM, 업데이트 |
| [10. 스마트 회의록 데모](docs/10-meeting-demo.md) | 노트북에서 실제 음성으로 — 모델 반입, 기동, 확인, 한계 |
| [11. 경쟁 제안서 대비 격차](docs/11-competitive-gap.md) | 없는 것을 없다고 적는다 — 제안 대응 리스크 |
| [04. AI 가상상담원 확장 설계](docs/04-ai-avatar-counselor.md) | 아바타 파이프라인, WebRTC, 단계별 구현 |
| [05. 데이터·보안·컴플라이언스](docs/05-data-security-compliance.md) | 망분리·KCMVP·개인정보·AI 리스크 통제 |
| [06. 기술 스택 & MVP 개발 계획](docs/06-tech-stack-mvp.md) | 블록 모노레포 구조, Wave별 개발 계획 — **코딩 착수용** |
| [07. GS인증 준비](docs/07-gs-certification.md) | 회의록 패키지 형상 고정, 심사 준비 상태(기계 보장 / 사람 손 필요) |

## 현재 구현 상태 (Wave 1~7 완료)

**고객이 질문하면 1초 안에 상담원 화면에 근거 팝업이 뜬다** — 사양서의 핵심 경로가 동작한다.

```
전화(RTP/G.711) ┐
마이크(WebSocket) ┴→ VAD → STT(+커스텀 사전) → 컴플라이언스 필터(<10ms)
      → 질의 추출(SLM) → 하이브리드 검색(Dense+BM25) → 리랭킹 → 지식 팝업
      → 통화 종료 시 자동 요약
```

| 블록 | 역할 | 상태 |
|------|------|------|
| `CORE-BUS` | 세션 레지스트리, 이벤트 버스 배선, Dual-Profile 컨텍스트 | ✅ |
| `CORE-GW` | JWT 인증, `/v1/audio/stream` WebSocket(사양서 §4), 데모 페이지 | ✅ |
| `AUD-VAD` | 발화 구간 분할(패딩·interim·행오버), VAD 어댑터(energy/silero) | ✅ |
| `STT-CORE` | `BaseSTTAdapter` ABC, fake/Faster-Whisper 어댑터 핫스왑 | ✅ |
| `FLT-MICRO` | PII 마스킹 + 컴플라이언스 룰 체크 (Fast-Path <10ms) | ✅ |
| `RAG-KB` | 문서 파서(**HWP**/HWPX/PDF/DOCX), 조항 경계 청킹, 임베딩, 색인, 문서 파기 | ✅ |
| `RAG-SRCH` | Qdrant Dense + BM25 Sparse RRF 융합, 리랭킹, **희소 색인 자가 복원** | ✅ |
| `TA-ASSIST` | 맥락 기반 질의 추출 → 검색 → 지식 팝업 (1초 예산) → **근거 검증을 통과한 추천 답변** | ✅ |
| `LLM-GW` | vLLM/상용 API 추상화, 프로파일별 모델 분기, **테넌트·월별 토큰 계측** | ✅ |
| `LLM-SUM` | 세션 종료 배치 요약 (AICC 표준요약 / 회의록·Action Item), **영속 저장·파기 API** | ✅ |
| `SCN-STUDIO` | **저작·학습 콘솔** — 룰 저작·즉시테스트·배포, STT 사전, 검색 튜닝, 평가셋, 채택률 | ✅ |
| `UI-AGENT` | **상담원 워크스페이스** — 자막·컴플라이언스 경고·지식 팝업·요약·채택 피드백 | ✅ |
| `AUD-RTP` | **전화 인입** — RTP 파싱·지터 버퍼·G.711 디코드·채널 분리, **끊긴 통화 회수** | ✅ |
| `SPK-DIA` | **화자분리** — 온라인 군집화로 참석자 식별 | ✅ |
| `UI-MEET` | **스마트 회의록** — 화자별 자막·참석자 발언량·안건/Action Item, **실명 영속** | ✅ |
| `CORE-LIC` | **오프라인 DRM** — H/W 지문, RSA-4096 서명 `.lic`, 블록·용량 게이팅 | ✅ |
| `CORE-SEC` | **감사·암호화** — 해시 체인 append-only 감사 로그, 봉투 암호화(KCMVP 연동 지점) | ✅ |
| `CORE-ADM` | **운영 콘솔** — 블록 상태·라이선스 만료·감사 무결성을 한 화면에 | ✅ |
| `TTS-CORE` | **음성 합성** — 문장 단위 스트리밍, 끼어들기, 한국어 수사 전처리 | ✅ |
| `BOT-VOICE` | **음성봇** — 시나리오 진행·슬롯·사람 연결 강제, **정규식→어휘→sLLM 의도 분류** | ✅ |
| `AVA-COUNSEL` | **아바타 가상상담원** — 입 모양 타임라인, 상태 전환 | ✅ |

### 고객사가 직접 운영하는 화면 (`SCN-STUDIO`)

온프레미스 B2B에서 룰 한 줄 바꾸는 데 공급사 인력이 필요하면 유지보수 원가가
라이선스 수익을 잠식한다. 그래서 저작 도구는 부가 기능이 아니라 수익성의 전제다.

| 화면 | 하는 일 |
|------|---------|
| 컴플라이언스 룰 | 초안 편집 → **실제 필터 블록으로 즉시 테스트** → 배포(파이프라인에 자동 반영) → 이력에서 되돌리기 |
| STT 사전 | 상품명·전문용어 등록 (범용 STT가 확실히 틀리는 고유명사 교정) |
| 검색 튜닝 | 질의를 넣어 Dense/Sparse/리랭킹 **점수 분해** 확인 — "왜 이 문서가 안 나오지"를 직접 진단 |
| 평가셋 | 골든셋으로 Recall@K·지연 측정 — 튜닝 전후 비교가 추측이 되지 않게 |
| 팝업 채택률 | 상담원이 실제로 썼는지 집계 + 개선 대상 질의 목록 |

`make up` 후 <http://localhost:8090/console>. 룰은 **초안 → 배포**를 명시적으로
거쳐야 반영되므로, 편집 중인 정규식이 상담 파이프라인에 새어 나가지 않는다.

### 품질 개선 루프가 실제로 닫혀 있다

```
저작 콘솔에서 상품명 사전 등록 / 컴플라이언스 룰 편집
   → 배포 채널(공유 저장소)로 발행 — 파일 복사도 재기동도 없다
   → STT-CORE가 읽어 인식 교정 (무배담 행복플러스 → 무배당 행복플러스보험)
   → FLT-MICRO가 읽어 테넌트별 룰 적용 (초안은 적용되지 않는다)
   → 상담원 워크스페이스에 교정된 자막 + 컴플라이언스 경고 표시
   → 상담원이 팝업 "도움됨/아님" 클릭
   → 저작 콘솔 채택률 + 개선 대상 질의 목록에 반영
```

저작 블록과 실시간 블록은 **서로를 호출하지 않는다.** 저작 도구가 꺼져 있어도
마지막 배포본으로 상담이 계속 돈다 — 업무시간에만 콘솔을 켜 두는 고객사를 위해서다.

| 화면 | 주소 |
|------|------|
| 고객 데모 (마이크→자막) | `:8080/demo` |
| 상담원 워크스페이스 | `:8091/workspace` |
| 스마트 회의록 | `:8094/minutes` |
| 저작·학습 콘솔 | `:8090/console` |
| 라이선스 현황·설치 API | `:8095/docs` |
| 감사 조회·무결성 검증 API | `:8096/docs` |
| **운영 콘솔** | `:8097/console` |
| **아바타 상담원** | `:8100/avatar` |

### 라이선스는 폐쇄망에서 파일 두 개로 끝난다 (`CORE-LIC`)

고객사 망은 인터넷에 나가지 못한다. 온라인 활성화도, call-home도 쓸 수 없으므로
**파일 자체가 진위를 증명해야 한다.**

```
① 설치 서버   POST :8095/internal/v1/license/request   → acme.req (H/W 지문 해시)
② USB 반출    acme.req → 공급사 발급 서버
③ 발급 서버   licgen issue acme.req --key license_key.pem --expires 2027-12-31 \
                  --block STT-CORE:concurrent_channels=50 --catalog-root .
④ USB 반입    acme.lic → 설치 서버
⑤ 설치 서버   POST :8095/internal/v1/license           → 검증 후 원자적 설치
```

- **라이선스 서버를 두지 않는다.** 모든 블록이 같은 `.lic`을 읽어 각자 검증한다 —
  중앙 검증 서버는 폐쇄망에서 가장 피해야 할 단일 장애점이다
- **지문은 N-of-M 일치.** NIC 하나 교체했다고 서비스가 멈추면 정상적인 유지보수가
  장애가 된다. 부품별 해시를 남겨 2개 이상 일치하면 통과하되, 다른 서버로의 복제는 거부한다
- **만료는 30일 유예, 지문 불일치는 즉시 거부.** 갱신이 며칠 늦었다고 상담을 끊으면
  고객사 업무가 멈춘다. 반대로 복제 허용은 DRM의 존재 이유를 없앤다
- **개인키는 이 레포 어디에도 없다.** 공개키만 릴리스 빌드가 내장한다
- `CORE-LIC`만 라이선스 없이 뜬다 — 설치기가 라이선스를 요구하면 최초 구축에서
  아무도 설치할 수 없다

업셀은 `.lic` 재발급으로 끝난다. 채널 수를 50→100으로 올리는 데 재설치가 필요 없다.

### 감사 로그는 지울 수 없어야 증거가 된다 (`CORE-SEC`)

금융·공공 조달에서 감사 로그는 통과 조건이고, 그 가치는 "변조되지 않았음을
증명할 수 있는가"에서 나온다. 지울 수 있는 로그는 증거로 쓰이지 못한다.

각 레코드가 앞 레코드의 해시를 품는다. 한 줄을 고치거나 지우면 그 뒤 모든 해시가
어긋나고, 검증 API가 **몇 번째 레코드에서** 끊겼는지까지 짚어 준다.

```
GET :8096/internal/v1/audit/verify
→ {"intact": false, "broken_at": 3,
   "reason": "레코드 내용이 자기 해시와 맞지 않는다 — 이 레코드가 변조됐다"}
```

기록 경로가 둘인 이유:

| 경로 | 쓰는 곳 | 왜 |
|------|---------|-----|
| `audit.log` 토픽 (비동기) | 세션 시작·종료, 문서 파기, 룰 배포 | 감사 저장소가 밀려도 상담은 계속 돈다 |
| `POST /internal/v1/audit` (동기) | PII 원문 열람, 녹취 반출 | **기록 성공 후** 열람을 허용해야 한다. 열람은 됐는데 기록은 실패한 상태가 남으면 감사에서 그 열람은 없었던 일이 된다 |

저장 암호화는 **봉투 방식**이다. 레코드마다 임의의 DEK로 본문을 암호화하고 DEK만
마스터 키로 감싼다 — 키 교체 시 DEK만 다시 감싸면 되므로 수백 GB의 녹취를
재암호화하지 않는다. 전체 재암호화를 요구하는 설계는 폐쇄망에서 사실상 교체 불가다.

암복호는 각 블록이 인프로세스로 한다(`vai_common.crypto`). CORE-SEC의 API로
제공하면 저장 경로마다 네트워크 왕복이 붙고, 이 블록이 전 블록의 병목이자
단일 장애점이 된다.

> **한계를 분명히 한다.** 해시 체인은 변조를 *막지* 않고 *탐지*한다. root 권한자는
> 파일 전체를 다시 쓸 수 있다. 완전한 방어는 head 해시를 외부에 정기 공증하는
> 운영 절차와 함께여야 성립한다. KCMVP 검증모듈은 고객사 조달품이므로 어댑터
> 연동 지점만 제공하며, **제공자 미지정 시 조용히 AES로 폴백하지 않는다.**

### 운영 콘솔은 편의 기능이 아니라 원가 구조다 (`CORE-ADM`)

폐쇄망에서는 공급사가 원격으로 들여다볼 수 없다. 고객사 운영자가 스스로
"어느 블록이 죽었는지, 라이선스가 언제 만료되는지, 감사 로그가 온전한지"를
확인하지 못하면 모든 확인이 **출동**으로 이어지고, 출동 원가가 유지보수 수익을
잠식한다. 그래서 이 화면은 매출이 아니라 원가 쪽 항목이다.

`make up` 후 <http://localhost:8097/console>.

| 첫 화면이 즉시 알려 주는 것 | 왜 |
|---|---|
| 정상 블록 수 / 전체 | 죽은 블록을 한눈에. **응답 없음(unknown)과 죽음(down)을 구분한다** — 방화벽 문제를 블록 장애로 오인하면 조사 방향이 통째로 어긋난다 |
| 라이선스 만료 잔여일 | 30일 이내면 배너. 만료는 예고 없이 오면 사고, 미리 보이면 그냥 갱신 업무다 |
| 서명 검증 여부 | **꺼져 있으면 빨간 배너.** 서명 검증 없이 도는 빌드가 납품되는 것이 라이선스 사고의 대표 경로다 |
| 감사 무결성 + 끊긴 지점 | 감사 대응의 첫 화면 |
| 저장 암호화 활성 여부 | 제안서에는 있는데 현장에서는 꺼져 있는 상태를 잡아낸다 |

설계에서 지킨 것:

- **모든 블록을 동시에, 2초 상한으로 찌른다.** 순차로 돌면 죽은 블록마다 타임아웃이
  쌓여 화면이 멈춘다 — 장애 상황에서 가장 느려지는 화면이 장애 확인용 화면이면
  없는 것만 못하다
- **일부 조회가 실패해도 나머지는 그린다.** CORE-LIC이 죽었다고 블록 상태 표까지
  사라지면 운영자는 아무 단서도 못 얻는다
- **집계만 하고 저장하지 않는다.** 콘솔이 상태를 따로 저장하면 곧 원본과 갈라져
  "화면은 정상인데 실제로는 죽어 있는" 상태가 된다
- **인증은 게이트웨이와 같은 검증기를 쓴다**(`vai_common.auth`). 콘솔이 자체 인증을
  따로 두면 스코프 해석이 갈라지고, 게이트웨이에서 막힌 권한이 콘솔에서는 통과한다.
  상담원 토큰(`session:create`)으로는 들어올 수 없다
- **기본 비밀키로는 기동을 거부한다.** "나중에 바꾸겠다"로 남는 기본값이 이 영역의
  가장 흔한 취약점이다. 데모는 `VAI_ADM_REQUIRE_AUTH=false`로 **명시적으로** 끈다 —
  껐다는 사실이 설정에 남는 편이 낫다
- **거부된 접근과 감사 열람 자체가 감사에 남는다.** 성공만 남기면 침해 시도가
  보이지 않고, 감사 기록을 훑어 본 사실이 흔적 없이 사라진다

### 반입 목록은 사람이 만들지 않는다 (에어갭 번들)

폐쇄망 반입은 되돌릴 수 없다. USB를 들고 들어간 뒤 "이미지 하나가 빠졌다"를
알게 되면 반출입 승인을 다시 밟아야 하고, 고객사에 따라 며칠이 걸린다.
그래서 담을 목록을 **라이선스와 카탈로그에서 계산한다.**

```bash
make bundle-plan LICENSE=customer.lic     # 무엇이 담기는지 먼저 확인
make bundle      LICENSE=customer.lic     # dist/visionai-0.1.0.tar.gz
```

계산이 잡아 주는 것 — 전부 손으로 세면 빠지는 것들:

| 규칙 | 예 |
|---|---|
| **의존의 의존**까지 폐포 | `UI-AGENT` 하나 → 12블록. 직접 의존은 보이지만 의존의 의존은 안 보인다 |
| **이벤트 생산자** (생산자가 하나뿐일 때) | `UI-MEET`는 `speaker.label`을 소비한다 → `SPK-DIA`가 없으면 화자 라벨이 영원히 안 붙는다. `depends_on`에는 안 나타난다 |
| **기능 의존**(`recommends`) | `UI-AGENT`는 `TA-ASSIST` 없이도 뜬다 — 자막은 나오고 **지식 팝업 패널만 죽는다.** 담되 "라이선스 범위 밖"으로 따로 보고한다 |
| **인프라 이미지** | 검색 블록이 있으면 qdrant, 버스를 쓰면 redis. compose와 같은 태그를 쓰는지 테스트가 강제한다 |
| **CORE-LIC은 무조건** | 라이선스를 설치하는 블록이 번들에 없으면 최초 구축에서 아무것도 기동 못 한다 |

생산자가 여럿인 토픽(`audit.log`)은 **자동으로 다 담지 않는다.** 전부 담으면
라이선스 범위 밖 이미지가 무더기로 들어가므로, 하나도 안 담겼을 때만 경고한다.

설치기는 순서를 고정한다 — 앞 단계가 실패하면 뒤를 시작하지 않는다:

```
사전 점검(docker·디스크·포트·구성파일) → 무결성 대조(SHA256) → 적재 → 기동 → 자가진단
```

`./install.sh --dry-run`은 docker를 건드리지 않고 점검만 한다 — **반입 전에**
빌드 서버에서 돌려 볼 수 있다. 체크섬이 어긋나면 적재를 시작하지 않는다:
반쯤 깨진 이미지를 로드하고 원인 모를 장애를 쫓는 것이 최악이다.

재설치(업그레이드)에서는 우리 자신이 포트를 쓰고 있으므로, 기존 설치를 감지하면
포트 충돌을 실패가 아니라 업그레이드로 본다 — 폐쇄망에서 업그레이드는 재설치가
유일한 수단이라 이 경로가 막히면 치명적이다.

자가진단은 실패마다 **어디를 보라고** 알려 준다. 실패 목록만 던지는 진단은
결국 공급사 전화로 이어진다.

### 약관은 평문으로 오지 않는다 (문서 파서)

공공은 HWP, 금융은 PDF/DOCX다. **HWP를 못 읽으면 공공 조달 입찰 자체가 막힌다** —
"약관 파일을 넣어 보세요"라는 PoC 첫 단계에서 끝난다.

```bash
curl -F file=@약관.hwp -F tenant_id=acme :8086/internal/v1/kb/card/documents/upload
```

| 형식 | 방식 | 비고 |
|---|---|---|
| HWP 5.0 | CFB 컨테이너 → raw deflate → 레코드 파싱 | **제어 문자 폭**을 정확히 건너뛰는 것이 전부다. 확장 컨트롤은 16바이트인데 한 글자로 세면 그 뒤 텍스트가 통째로 깨져 그 문서는 검색에서 영원히 안 나온다 |
| HWPX·DOCX | zip + XML (**표준 라이브러리만**) | 에어갭 반입에서 의존성 하나는 검토 대상 하나다. 네임스페이스가 아니라 지역명으로 태그를 찾아 한글·워드 버전에 묶이지 않는다 |
| PDF | pypdf 텍스트 레이어 | |
| 평문 | UTF-8 → **CP949** → EUC-KR 순 | 국내 문서는 CP949 저장이 아직 많다. UTF-8만 시도하면 멀쩡한 약관이 "읽을 수 없음"이 된다 |

**실패를 성공으로 위장하지 않는다.** 빈 텍스트로 색인이 "성공"하면 고객사는
약관을 넣었는데 검색이 안 되는 이유를 영영 모릅니다. 그래서 400으로 즉시 알립니다:

- 스캔 PDF → `"텍스트 레이어가 없다 — OCR을 거친 사본이 필요하다"`
- 배포용 HWP → `"복호화 키가 필요해 읽을 수 없다. 원본 문서를 요청한다"`
- 암호 걸린 문서 → `"암호를 푼 사본이 필요하다"`

온전히 읽지 못한 부분은 문서에 경고로 남깁니다(표 안의 글자, 이미지 페이지,
머리말). 조용히 넘기면 "왜 이 조항만 검색이 안 되지"를 아무도 설명하지 못합니다.

확장자가 틀려도 **매직 바이트로 판별**합니다 — 고객사 파일은 확장자가 자주
틀리고(HWPX를 .hwp로 저장), 그 반려를 고객사는 제품 결함으로 봅니다.

### 산 만큼만 쓴다 (동시 채널 한도 강제)

`.lic`에 채널 수를 적어 놓고 아무도 세지 않으면 그 숫자는 장식입니다.
CORE-BUS가 세션 인입 시점에 셉니다.

```
#1~#3 → 201    #4 → 429  "동시 채널 한도(3)에 도달했다 — TA-ASSIST 라이선스 기준"
```

- **가장 빡빡한 블록이 상한을 정합니다.** TA-ASSIST를 50채널만 샀는데 STT를
  100채널 샀다고 100건을 받으면 51번째부터 **팝업 없는 상담**이 됩니다.
  계약과 동작이 어긋나는 쪽이 훨씬 나쁩니다.
- **어느 블록이 막았는지 응답에 담습니다.** 운영자가 "왜 통화가 안 받아졌나"를
  물었을 때 답이 있어야 증설 견적으로 이어집니다.
- **거부는 로그가 아니라 감사에 남습니다.** 놓친 상담은 매출이고, 매출 손실이
  로그 파일에만 있으면 아무도 못 봅니다.
- **TTL을 넘긴 세션은 걷어냅니다.** 종료 이벤트를 놓친 세션이 채널을 계속 잡으면
  한도가 조금씩 새어 나가고, 결국 라이선스 안에서 상담을 못 받는 상태가 됩니다 —
  원인을 찾기 가장 어려운 종류의 장애입니다.

콘솔은 사용률 70%에서 노랑, 90%에서 빨간 배너를 띄웁니다. 상한에 부딪힌 뒤에
아는 것은 이미 상담을 놓친 뒤입니다.

### 배포는 블록을 켜고 끄는 것 (Helm/K8s)

```bash
helm install visionai deploy/charts/visionai -f values-aicc.yaml \
  --set-file license.content=customer.lic
```

| 패키지 | 블록 | 인프라 |
|---|---|---|
| `values-aicc.yaml` | 16개 (전화 인입 → 지식 팝업 → 요약) | redis + qdrant |
| `values-meeting.yaml` | 11개 (화자분리 → 회의록) | redis |
| `values-voicebot.yaml` | 18개 (음성봇 1차 응대 → 사람 연결) | redis + qdrant |
| `values-avatar.yaml` | 18개 (키오스크·웹 아바타 상담) | redis + qdrant |

**블록 하나 = values 항목 하나.** 그 목록이 라이선스(`.lic`)·에어갭 번들 목록과
같아야 합니다 — 셋이 갈라지면 "돈은 받았는데 안 도는" 블록이 생깁니다.
`blockctl bundle-plan --license customer.lic`이 대조 근거입니다.

폐쇄망을 전제로 한 선택들:

- **의존 차트를 두지 않습니다.** helm repo에 나갈 수 없어 `bitnami/redis`는
  설치 자체가 불가능합니다. redis·qdrant를 차트가 최소 구성으로 담고, 고객사가
  이미 운영 중이면 `infra.*.enabled=false`로 끕니다
- **`pullPolicy: IfNotPresent`** — 레지스트리가 없습니다. `Always`면 전 블록이
  기동에 실패합니다
- **`CORE-SEC`·`CORE-LIC`은 `replicas`를 올려도 1로 강제**됩니다. 복제하면
  감사 해시 체인이 끊기거나 라이선스 파일이 깨집니다. "성능을 올리려다 감사
  로그를 못 쓰게 만드는" 사고를 설정 실수로 낼 수 있게 두지 않았습니다
- **라이선스 볼륨은 `CORE-LIC`만 쓰기 가능**합니다. 나머지 블록은 읽기 전용이라
  자기 라이선스를 고쳐 쓸 수 없습니다
- **라이선스·감사 PVC는 `helm uninstall`로도 안 지워집니다**(`resource-policy: keep`).
  라이선스가 지워지면 발급 절차를 처음부터 다시 밟아야 하고(폐쇄망에서 며칠),
  감사 로그가 지워지는 것은 그 자체로 감사 사고입니다

차트가 카탈로그에서 표류하는 것은 테스트가 막습니다 — 블록을 추가하고 차트를
잊으면 배포할 수 없는 블록이 생기고, 그 사실은 배포 당일(=검수일)에 드러납니다.

### 릴리스 빌드가 서명 검증을 켠다

개발 체크아웃과 릴리스 빌드의 차이는 **딱 하나 — 공개키가 내장돼 있는가**입니다.

```bash
make release PUBLIC_KEY=/secure/keys/license_pub.pem
```

그 하나가 빠지면 서명 검증이 꺼진 채로 나갑니다. 그런데 **이 상태는 겉으로
완전히 정상으로 보입니다** — 블록은 뜨고 상담은 돌고 아무 오류도 없습니다.
사람이 눈으로 확인할 수 없으니 기계가 막습니다:

```
$ blockctl release-check
[block] 공개키가 내장되지 않았다 — 서명 검증이 꺼진 채로 납품된다
[block] 발급기 'licgen'가 릴리스에 포함돼 있다 — 공급사 전용이다
✗ 납품 불가 — 차단 항목 2건
```

| 검사 | 왜 차단인가 |
|---|---|
| 공개키 내장 + RSA-4096 이상 | 없으면 라이선스가 장식이 된다 |
| **개인키 유출** | 고객사가 무제한 라이선스를 발급할 수 있고, 이미 나간 모든 라이선스가 무의미해진다. 키 교체는 전 고객사 재반입을 뜻해 폐쇄망에서는 사실상 불가능 — **한 번 새면 되돌릴 수 없다** |
| 발급기(`licgen`) 혼입 | 개인키만 구하면 라이선스를 찍어낼 수 있는 상태가 된다 |
| `.env`·`master.key` 등 비밀 파일 | |
| `block.yaml` ↔ `pyproject` 버전 일치 | 어느 버전이 배포됐는지 모르면 장애 재현도 회귀 확인도 못 한다 |

소스 복제는 `git archive`를 씁니다 — 추적되는 파일만 담으므로 로컬에 굴러다니는
키·`.env`가 딸려 나갈 수 없습니다. `cp -r`이었다면 반드시 뭔가 새어 나갑니다.

**오탐을 내지 않습니다.** 개인키 탐지는 BEGIN/END 쌍과 본문까지 갖춰야 잡습니다 —
BEGIN 문자열만 찾으면 탐지 패턴을 상수로 가진 소스 파일이 걸립니다. 보안 게이트의
오탐은 단순한 불편이 아니라, 사람이 경고를 무시하게 만들어 진짜 유출도 지나치게 합니다.

검증한 것: 릴리스 트리에서 `resolve_public_key(None)`이 내장 키를 찾아 서명
검증이 켜지고, 라이선스가 없으면 기동을 거부하며, 다른 키로 서명한 라이선스는
거부됩니다.

### AI 음성상담원 — 같은 코어에 입이 붙는다

```
[기존] 전화 → VAD → STT → 필터 → 검색/sLLM → 상담원 화면
[추가]                          ↓
                          BOT-VOICE (시나리오) → TTS-CORE → 전화
                          AUD-VAD ──끼어들기──────┘
```

```bash
helm install visionai deploy/charts/visionai -f values-voicebot.yaml
```

**첫 소리까지의 지연이 이 영역의 전부입니다.** 사람은 0.5초 안에 반응이 없으면
"여보세요?"를 다시 합니다. 답변 전체를 합성하면 첫 소리가 문장 수만큼 늦으므로,
문장 단위로 잘라 첫 조각을 가장 짧게 만듭니다. 실측 **첫 소리 16ms**(96자 안내).

**끼어들기는 선택 기능이 아닙니다.** 고객이 말을 시작했는데 봇이 계속 떠들면
그 통화는 실패합니다. 실측: 전체의 25% 지점에서 취소 → **한 조각도 더 나가지 않음**.
"거의 멈췄다"는 멈춘 것이 아닙니다.

**한국어 전처리가 없으면 음성봇은 쓸 수 없습니다.** "50,000원"이 "오영영영영원"으로
나가면 고객은 금액을 알아듣지 못합니다. 특히 고유어/한자어 수사 구분:

| 입력 | 출력 | 틀리면 |
|---|---|---|
| `50,000원` | 오만 원 | 금액을 못 알아듣는다 |
| `3개` | **세** 개 | "삼 개"는 듣자마자 기계다 |
| `3시 30분` | **세** 시 **삼십** 분 | 한 문장에 둘이 섞인다 |
| `45분 걸립니다` | **사십오** 분 | "마흔다섯 분"은 사람 수가 된다 |
| `010-1234-5678` | 공일공 일이삼사 오육칠팔 | 붙여 읽으면 받아 적지 못한다 |
| `1억 5천만원` | 일억 오천만원 | "일 억 오 천만원"으로 끊긴다 |

엔진 바깥에 둡니다 — 엔진을 바꿔도 읽는 방식은 그대로여야 합니다.

**봇에서 가장 자주 깨지는 것은 모델이 아니라 막다른 골목입니다.** 못 알아들어
되묻고, 또 되묻고, 고객은 끊습니다. 시나리오 배포 전에 강제로 검사합니다:

```
PUT /internal/v1/scenarios/acme  →  400
{"problems": ["사람 상담원으로 갈 수 있는 경로가 없다",
              "ask_purpose: 재질문 문구가 없다"]}
```

- 재질문 상한을 넘으면 사람에게 넘깁니다 (무한 재질문이 최악의 경험)
- 되묻는 말은 처음과 달라야 합니다 (같은 문장 반복 = 고장 난 것처럼 들림)
- 도달 가능한 사람 연결 경로가 없으면 **배포를 거부**합니다
- 시나리오 순환을 감지하면 사람에게 넘깁니다

대화 상태는 봇 프로세스 **밖에** 있습니다 — 통화 중 배포가 곧 통화 종료인
설계는 운영할 수 없습니다.

### 아바타는 서버가 그리지 않는다 (`AVA-COUNSEL`)

```
TTS-CORE ──오디오 조각──┐
                       ├─→ 한 소켓 → 브라우저·키오스크가 렌더링
AVA-COUNSEL ─입 모양 타임라인─┘
```

**영상을 서버에서 만들면 통화 100개에 GPU 100장이 듭니다.** 인코딩·전송 지연도
음성 위에 더해집니다. 서버는 입 모양 타임라인만 보내고 — 조각당 수십 바이트 —
그리는 것은 클라이언트가 합니다.

**동기가 전부입니다.** 소리와 입이 어긋나면 사람은 즉시 알아채고, 어긋난 아바타는
없느니만 못합니다. 그래서 타임라인을 TTS의 **실측 길이**에 맞춰 계산합니다 —
글자 수로 추정하면 문장이 길어질수록 누적돼 끝에서 눈에 띄게 벌어집니다.

실측(49조각 발화): **식별자·길이 불일치 0개, 오디오 길이를 넘는 프레임 0개.**

한글은 음절이 초성·중성·종성으로 분해되고 그 구조가 유니코드에 들어 있어,
발음 사전 없이 입 모양을 뽑을 수 있습니다(영어라면 사전이 필요합니다):

| 음절 | 입 모양 | 왜 |
|---|---|---|
| 마 | **pp** → aa | 양순음에서 입술이 닫힌다. 이게 틀리면 즉시 가짜로 보인다 |
| 밥 | pp → aa → **pp** | 받침도 넣는다. 없으면 "바"처럼 보인다 |
| 아 | aa | 초성 ㅇ은 소리가 없다. 넣으면 없는 자음이 보인다 |

양순음에는 다른 자음보다 긴 시간(45% vs 30%)을 줍니다 — 입술 닫힘이 짧으면
눈에 안 보이고, 그러면 "마"가 "아"로 보입니다.

**말하지 않을 때도 멈추지 않습니다.** 정지한 아바타는 화면이 얼어붙은 것처럼
보이고 고객은 통화가 끊겼다고 여깁니다. 발화가 끝나면 `listening`으로 돌아가고,
눈 깜빡임은 무작위 간격입니다(일정한 주기는 기계처럼 느껴집니다).

### 두 개의 제품 패키지가 같은 코어를 쓴다

```
                      ┌→ [AICC]    FLT-MICRO → TA-ASSIST → UI-AGENT
전화/마이크 → VAD → STT ┤
                      └→ [회의록]  SPK-DIA → UI-MEET
                                    ↘ 공통: LLM-SUM(요약)
```

프로파일(`AICC`/`MEETING`)이 세션 생성 시 정해져 파이프라인 전체로 전파된다.
화자분리는 회의 프로파일에서만 돌아 AICC에서 GPU를 낭비하지 않는다 —
상담은 채널(고객/상담원)로 이미 화자가 갈려 있기 때문이다.

```bash
make install          # uv 워크스페이스 동기화
make check            # 린트 · 블록 경계 · 타입 · 테스트 · 카탈로그 검증
make up && make demo  # compose 기동 → http://localhost:8080/demo
```

테스트는 **GPU도 Redis도 없이** 파이프라인 전체를 검증한다(인메모리 버스 + fake/hashing 어댑터).
Redis가 있으면 블록들을 실제 프로세스로 띄우는 스택 스모크 테스트까지 함께 돈다.

레고블록 원칙은 문서상의 약속이 아니라 CI 게이트다 — `import-linter`가 블록 간 직접
import, 계층 역전, 서비스 로직의 엔진 직접 참조, 상담 경로에서의 외부 SDK 사용을
차단하고, `blockctl check`가 매니페스트·의존·토픽 생산자·청구 단위 정합성을 검사한다.

### 품질은 인상이 아니라 수치로 관리한다

"좋아진 것 같다"로는 튜닝의 이득도 회귀도 증명되지 않는다. 골든셋과 기준선을
레포에 두고 매 커밋마다 대조한다.

```bash
uv run evalctl run -v        # 검색·PII·TTS 골든셋 실행 + 실패 사례
uv run evalctl compare       # 기준선 대비 회귀가 있으면 exit 1
uv run evalctl baseline --note "왜 바꿨는지"   # 의도한 변화일 때만
```

| 스위트 | 재는 것 | 지표 |
|---|---|---|
| `retrieval` | 상담 발화로 약관 조항을 찾는가 | Recall@3 · **MRR** · nDCG@3 |
| `pii` | 못 지운 것과 과하게 지운 것 | 정확도(100%가 기준) |
| `tts` | 한국어 수사 읽기(고유어/한자어) | 정확도(100%가 기준) |
| `grounding` | 지어낸 답변을 걸러내는가 / 멀쩡한 답변을 버리는가 | 정확도(100%가 기준) |
| `intent` | 음성봇이 고객 말을 옳은 갈래로 보내는가 | 정확도 · **오라우팅 0건** |

**Recall@K만 보면 회귀를 놓친다.** 정답이 1위에서 3위로 밀려도 Recall@3은 1.0
그대로다 — MRR을 함께 보는 이유이고, 게이트가 MRR 하락에서 실패하는 이유다.
사례 수가 줄어드는 것도 회귀로 본다(실패하는 사례를 지워 초록을 만드는 경로를 막는다).
지연은 러너 성능차가 커서 **게이트로 쓰지 않고 경고만** 낸다 — 매번 뜨는 빨강은
진짜 회귀까지 같이 무시하게 만든다.

도입 첫날 골든셋이 잡아낸 실제 결함 셋 — 셋 다 조용히 잘못 돌던 종류다:

1. `010-1234-5678로`처럼 **조사가 숫자에 붙으면 PII가 마스킹되지 않았다**(정규식
   `\b`는 숫자와 한글 사이에 경계를 만들지 않는다). 상담 STT 출력에서는 조사가
   붙은 형태가 오히려 다수다.
2. **하이브리드 융합이 희소 전용 히트를 버렸다** — BM25가 1위로 올린 조항도
   의미 벡터 상위권에 없으면 결과에서 사라졌다. 그러면 희소 축을 둘 이유가 없다.
3. **구어와 약관어가 만나지 못했다** — "잃어버렸어요"와 "분실"은 글자가 하나도
   겹치지 않는다. 질의 확장 사전으로 다리를 놓았다.

셋을 고친 효과: Recall@3 0.929 → **1.000**, MRR 0.865 → **0.948** (42건).
`evalctl run --suite retrieval --no-expand`로 확장의 기여만 따로 잴 수 있다.

### 추천 답변은 검증을 통과해야 화면에 올라간다 (`TA-ASSIST`)

상담원 화면에 뜬 문장은 상담원이 그대로 읽는다. 지어낸 숫자 하나가 곧바로
고객에게 전달되고, 금융 상담에서 그것은 불완전판매다. **틀린 답변은 답변이
없는 것보다 나쁘다** — 근거 원문만 보여주면 상담원이 직접 읽고 판단하지만,
그럴듯한 오답은 그 판단 자체를 건너뛰게 만든다.

sLLM이 만든 문장은 다음을 모두 통과해야 화면에 오른다. 하나라도 걸리면 버리고
근거 원문만 남는다.

| 검사 | 무엇을 막는가 |
|---|---|
| 인용 표기 `[n]` 존재·범위 | 근거 없는 문장, 없는 조항 인용 |
| **인용한 근거 안에 답변의 모든 숫자가 있는가** | "3,000원"이 "5,000원"이 되는 것 |
| 길이 상한(두 문장) | 통화 중에 못 읽는 분량 |
| 확약 표현("반드시", "무조건") | 조건부 약관을 무조건으로 바꿔 읽는 것 |
| 어휘 일치율 | 숫자는 맞는데 근거에 없는 문장을 통째로 지어낸 경우 |

**근거를 먼저 띄우고 답변은 뒤따라 채운다.** 생성을 기다리다 근거까지 늦어지면
1초 예산 안에 화면에 아무것도 없다. 모델이 없으면(echo 어댑터) 인용을 못 만들어
전부 폐기된다 — 답변이 안 뜨는 것이 올바른 동작이다.

## 빠른 이해를 위한 그림 한 장

```mermaid
graph LR
    subgraph Blocks["블록 카탈로그 (독립 개발·독립 과금)"]
        S["음성 블록<br/>RTP·VAD·STT·TTS·화자분리"]
        I["지능 블록<br/>필터·RAG·Assist·sLLM요약"]
        U["UX 블록<br/>상담원UI·회의록UI·보이스봇·아바타"]
        C["기반 블록<br/>버스·게이트웨이·DRM·보안·콘솔"]
    end
    Blocks -->|"bundle.yaml 조립"| PKG["제품 패키지<br/>A: 금융 AICC<br/>B: 스마트 회의록<br/>C: AI 가상상담원"]
    PKG -->|"Helm/K8s + .lic"| ONPREM["온프레미스<br/>(폐쇄망)"]
    PKG -->|"동일 이미지 + 구독 토글"| SAAS["SaaS"]
```
